{
  "name": "design-system",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "shared/design-system/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project design-system --web",
  "targets": {
    "panda-gen": {
      "command": "panda --watch --cwd=shared/design-system"
    },
    "prepare": {
      "command": "panda codegen --cwd=shared/design-system/src/"
    },
    "panda-init": {
      "command": "panda init --postcss --cwd=shared/design-system"
    }
  }
}
